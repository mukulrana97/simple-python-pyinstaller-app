pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'python3 sources/Test.py'
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }
        }
    }
}